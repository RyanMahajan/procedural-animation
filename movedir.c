#ifndef MOVEDIR
#define MOVEDIR

#include "globals.h"

void moveDir(int x, int y){
	//TODO write equation to get normalized velocity vector and add to TRANSLATION SCALAR
	float dir = 
	XTRANSLATE[0] = dir[0];
}
#endif
